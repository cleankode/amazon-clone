import React from 'react'
import './Returns.css';

function Returns() {
    return (
        <div className="returns">
            <h1>Items in recent order (can be returned :) )</h1>
        </div>
    )
}

export default Returns
